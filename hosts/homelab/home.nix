{...}: {
  userSettings = {
    web = {
      enable = true;
      browser = "brave";
    };

    media = {
      enable = true;
    };

    shell = {
      enable = true;
      apps.enable = true;
    };
  };
}
