{...}: {
  userSettings = {
    desktop = {
      enable = true;
    };

    shell = {
      enable = true;
      apps.enable = true;
    };

    web = {
      enable = true;
      browser = "brave";
    };

    media = {
      enable = true;
    };

    documents = {
      enable = true;
    };
  };
}
